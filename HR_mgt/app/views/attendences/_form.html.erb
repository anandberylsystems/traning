<%= form_for(attendence) do |f| %>
  <% if attendence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attendence.errors.count, "error") %> prohibited this attendence from being saved:</h2>

      <ul>
      <% attendence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry_time %>
    <%= f.datetime_select :entry_time %>
  </div>

  <div class="field">
    <%= f.label :leave_time %>
    <%= f.datetime_select :leave_time %>
  </div>

  <div class="field">
    <%= f.label :attendence %>
    <%= f.text_field :attendence %>
  </div>

  <div class="field">
    <%= f.label :employee_id %>
    
    <%= f.collection_select :employee_id, @employees, :id, :name  %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
